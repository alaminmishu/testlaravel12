## #ddev-generated
## Adds the mongodb PHP extension so the app can connect to MongoDB directly.
RUN (timeout 120 apt-get update || true) && DEBIAN_FRONTEND=noninteractive apt-get install -y -o Dpkg::Options::="--force-confold" --no-install-recommends --no-install-suggests php8.3-mongodb
